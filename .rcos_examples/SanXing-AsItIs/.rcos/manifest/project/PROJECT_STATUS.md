# PROJECT_STATUS

## 当前阶段

phase: semantic_extraction

附注：

- 语义建模仍是当前主阶段
- 应用层当前只有最小运行骨架，不应误判为已进入完整功能实现阶段
- 当前推荐实现顺序见 `PROJECT_ROADMAP.md`

---

## 当前唯一目标（强约束）

将三省书 PDF 中的评分语义结构化为：

👉 可执行 scoring model

---

## 当前任务范围（Scope）

允许：

- 分析 PDF 内容
- 提取行为描述
- 构建评分结构（low / high / anchor）
- 定义数据结构
- 校正 RCOS 项目上下文，使其与当前仓库事实一致

禁止：

- UI 优化
- 功能扩展
- 架构重写
- 引入新模块

---

## 当前系统完成度

### 已完成

- 项目方向定义
- 技术路线初步确定（FastAPI + SQLite）
- 基础服务运行骨架（systemd socket activation）
- 基础 API 壳层（health endpoint）
- RCOS 项目级上下文初步建立
- 初始评分语义种子文件已存在
- 扫描件 PDF 参考源已纳入项目目录
- 已确认种子文件在结构层面自洽：
  - `morning / midday / evening`
  - 六度预读
  - 八正道预读
  - 12 个评分维度

### 未完成

- 八正道评分结构（核心缺口）
- 评分种子到代码结构的映射
- 基于扫描件 PDF 的逐项文字级校勘
- check-in 业务逻辑
- storage 实现
- UI 实现
- 完整 API schema
- PDF 导出

---

## 风险点

1. 评分语义未结构化 → 系统无法使用
2. 三省 vs 八正道结构关系未明确
3. AI 容易过早进入 UI / feature 优化
4. RCOS 规划模块与实际已实现模块容易混淆
5. 数据目录与 PDF 目录已存在，但当前多为占位状态
6. 扫描件 PDF 当前无法在本机直接 OCR，因此不能把 seed 中每一条措辞都当成已复核原文

---

## 下一步（必须）

👉 在现有 seed 作为工作基线的前提下，继续推进评分维度结构化，并明确标注哪些内容仍待 PDF 进一步核对

---

## 成功判定

当以下条件满足：

- 每个维度有明确：
  - 行为描述
  - 分数锚点（1–5）
- 可映射为数据结构
- 可用于 UI 渲染

则认为进入下一阶段
